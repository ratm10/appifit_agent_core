name: appfit_core
description: AppFit shared package for auth and socket communication
version: 1.0.3

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ">=2.5.1 <4.0.0"
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^10.0.0
  encrypt: ^5.0.3
  crypto: ^3.0.3
  web_socket_channel: ^3.0.0
  connectivity_plus: ^6.0.0
  flutter_downloader: ^1.12.0
  app_installer: ^1.3.1
  path_provider: ^2.1.5
  package_info_plus: ^9.0.0
  sentry_flutter: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
